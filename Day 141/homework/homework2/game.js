import mainCharacter from "./characters.js";
import { warrior as tank, healer as support } from "./characters.js";

console.log(
  `Party: ${mainCharacter} (Main), ${tank} (Tank), ${support} (Support)`
);
